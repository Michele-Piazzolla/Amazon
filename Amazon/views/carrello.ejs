<!DOCTYPE html>
<html>
<head>
  <title>Carrello</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-light">
  <nav class="navbar navbar-dark bg-black mb-4">
    <div class="container">
      <a class="navbar-brand" href="/">Home</a>
      <a class="nav-link text-light" href="/carrello">
        🛒 Carrello (<%= carrelloCount %>)
      </a>
    </div>
  </nav>

  <div class="container">
    <% if (carrello.length === 0) { %>
      <div class="alert alert-info">
        Il carrello è vuoto. <a href="/" class="alert-link">Torna allo shopping</a>
      </div>
    <% } else { %>
      <h2 class="mb-4">Il tuo carrello</h2>
      <ul class="list-group mb-3">
        <% carrello.forEach(item => { %>
          <li class="list-group-item bg-secondary d-flex justify-content-between">
            <div>
              <%= item.nome %> - €<%= item.prezzo.toFixed(2) %>
            </div>
            <form method="POST" action="/rimuovi-carrello/<%= item.id %>">
              <button type="submit" class="btn btn-sm btn-danger">X</button>
            </form>
          </li>
        <% }) %>
      </ul>
      <div class="alert alert-primary">
        Totale: €<%= totale.toFixed(2) %>
      </div>
      <a href="/" class="btn btn-primary">Continua acquisti</a>
    <% } %>
  </div>